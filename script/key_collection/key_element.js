/**
	A KeyElement is an object which has a key. That's about it.
	It's the fundemental interface bloc for everything else.
	@author laifrank2002 
	@date 2020-01-03
 */
function KeyElement(key)
{
	this.key = key;
}